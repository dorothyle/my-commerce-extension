"use strict";
/*************************************************************************
 * Copyright 2025 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Solution-facing API for WebMCP tool registration.
 *
 * Solutions import this module to register browser-side tools that AO2
 * can invoke. The tool handler functions execute locally in the APP iframe;
 * only the serializable definition crosses postMessage to Core.
 *
 * ***Import:***
 *
 * ```typescript
 * import mcp from '@adobe/exc-app/mcp';
 * ```
 *
 * ***Default export:***
 *
 * [MCPApi](../interfaces/mcp.mcpapi.md#interface-mcpapi)
 *
 * ***Usage:***
 *
 * ```typescript
 * // Register a custom tool
 * mcp.registerTool(
 *   {
 *     name: 'getSelectedAssets',
 *     description: 'Returns the currently selected assets in the DAM',
 *     inputSchema: {
 *       type: 'object',
 *       properties: {
 *         includeMetadata: {
 *           type: 'boolean',
 *           description: 'Whether to include full metadata'
 *         }
 *       }
 *     }
 *   },
 *   async (args) => {
 *     const assets = await myApp.getSelectedAssets(args.includeMetadata);
 *     return { assets };
 *   }
 * );
 *
 * // Unregister a tool
 * mcp.unregisterTool('getSelectedAssets');
 * ```
 * @packageDocumentation
 * @module mcp
 */
const Global_1 = require("./src/Global");
const mcp = {
    executeTool: (name, args, targetFrameId) => {
        return (0, Global_1.getImpl)('mcp')().executeTool(name, args, targetFrameId);
    },
    getTools: () => {
        return (0, Global_1.getImpl)('mcp')().getTools();
    },
    registerTool: (definition, handler) => {
        return (0, Global_1.getImpl)('mcp')().registerTool(definition, handler);
    },
    unregisterTool: (name) => {
        return (0, Global_1.getImpl)('mcp')().unregisterTool(name);
    }
};
exports.default = mcp;
//# sourceMappingURL=mcp.js.map